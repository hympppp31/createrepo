# checking
<p>STEP 1 = if create a folder/repo locally 1st add it to git (init) then create the files with in it , then add and commit<p>
<P>STEP 2 = create a repo remotely on github with same name , then link it with the local one (git remote add origin LINK), do not add readme while creating cuz you have to pull the file again <p>
<p>STEP 3 = push <p>
<p>NOTE = step 1 and 2 can be done alternatively but step 3 should be done always atlast <p>
